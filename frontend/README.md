# 《基于大语言模型的智能新闻摘要与协同互动系统》前端工程

## 一、前端项目说明

`frontend` 是本系统的前端工程，当前处于新闻模块持续完善阶段。页面整体采用“顶部导航栏 + 左侧分类栏 + 主内容区”的布局方式，新闻首页、新闻详情页与互动功能已经接入 mock 后端接口。

## 二、技术栈

- Vue 3
- Vite
- TypeScript
- Vue Router
- Pinia
- Element Plus
- Axios

## 三、目录结构说明

```text
src/
├─ api/                # 接口封装
├─ assets/             # 静态资源
├─ components/         # 公共组件
│  ├─ layout/          # 顶部导航栏、侧边栏等布局组件
│  ├─ news/            # 新闻展示相关组件
│  └─ interaction/     # 点赞、收藏、评论等互动组件
├─ layouts/            # 页面整体布局
├─ router/             # 路由配置
├─ stores/             # Pinia 状态管理
├─ styles/             # CSS 变量与全局样式
├─ utils/              # 工具函数
└─ views/              # 页面视图
```

## 四、当前已完成内容

1. Vue 3 项目初始化
2. 路由配置
3. Pinia 状态管理
4. Element Plus 引入
5. Axios 请求封装
6. 统一主布局
7. 顶部导航栏
8. 左侧分类栏
9. 明暗模式切换
10. 基础页面占位
11. Mock 登录与用户状态管理
12. 路由守卫与角色权限控制
13. 首页新闻列表、热榜和搜索联动
14. 新闻详情页基础展示
15. 新闻详情页点赞、收藏、评论、回复、评论点赞
16. 新闻详情页跳转 AI 标题摘要生成页

## 五、运行方式

```bash
npm install
npm run dev
```

启动成功后，通常访问：

```text
http://localhost:5173
```

## 六、当前注意事项

1. 当前不连接数据库。
2. 当前不直接调用 `ai-service`。
3. 当前不实现真实登录和真实密码加密。
4. 当前新闻、详情、互动数据来自 backend mock 接口。
5. 当前互动状态和评论数据为 mock 内存更新，服务重启后不会持久化。
6. 当前所有页面仍以基础框架和占位内容为主，真实业务会在后续阶段继续完善。

## 七、用户与权限 Mock

当前已支持：

- Mock 登录
- 用户状态管理
- 路由守卫
- 角色权限控制
- 顶部导航栏登录状态展示
- 退出登录

测试账号：

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 普通用户 | `user` | `123456` |
| 审核编辑 | `editor` | `123456` |
| 管理员 | `admin` | `123456` |

## 八、首页模块说明

首页当前已接入：

- 新闻分类
- 新闻列表
- 搜索
- 热榜

首页数据来自 backend mock 接口，不连接数据库，也不直接调用 `ai-service`。

首页交互说明：

- 点击新闻卡片跳转 `/news/:id`
- 点击 AI 工具入口跳转 `/ai/title-summary`

## 九、新闻详情页说明

新闻详情页当前已支持：

- 新闻详情展示
- 新闻浏览记录
- 相关文章与推荐阅读
- 跳转 AI 标题摘要生成页
- 点赞
- 取消点赞
- 收藏
- 取消收藏
- 评论列表展示
- 发布评论
- 回复评论
- 评论点赞

未登录用户进行互动时会跳转登录页。

当前互动数据来自 backend mock 接口，不连接数据库，互动状态重启后不会持久化。

## 十、下一阶段

后续将继续完善 AI 生成页、社区模块、个人中心模块和后台模块，并逐步补齐更完整的业务功能。


## 个人中心模块补充说明

- 当前个人中心已支持浏览历史、收藏记录、评论记录和 AI 生成记录展示。
- 个人中心数据来源于 backend 接口，数据库优先，mock 兜底。
- 评论记录已与后端 `/api/profile/comments` 对齐。

## AI 生成记录补充说明

- 当前 `POST /api/ai/generate` 会在生成成功后同步写入 `ai_generate_record` 表。
- `GET /api/ai/records` 和个人中心 AI 生成记录会优先读取数据库，数据库不可用时回退 mock。
- 从新闻详情页导入的 AI 生成场景会携带 `source=news`、`source_news_id` 和 `source_title`，方便在个人中心区分来源。

## Timeline 模块补充说明

- 首页、社区和新闻详情页均可打开 Timeline 抽屉。
- Timeline 数据来自 backend 的 `/api/timeline/*` 接口，不直接调用 ai-service。
- 当前 Timeline 已支持数据库优先读取，数据库异常时仍可回退 mock。

## 社区模块补充说明

- 当前社区页面已支持数据库优先读取帖子、评论和热搜话题。
- 点赞、收藏、评论、回复、拉黑等操作会优先写入后端数据库，失败时回退 mock。
- 社区页面仍然只调用 backend，不直接连接数据库。
## DB12 真实新闻展示说明

- 首页新闻列表已支持展示数据库新闻的 `cover_image`。
- 有封面图的新闻显示图文卡片；没有封面图或图片加载失败时，使用无图卡片样式，不会显示破图。
- 左侧新闻分类会通过 `/home?category_id=xxx` 联动首页列表，请求后端 `/api/news` 重新筛选数据。
- 新闻详情页支持展示封面图；如果 `cover_image` 为空，则不显示图片区域。
- Timeline 入口依赖新闻的 `topic_id`，真实爬取新闻未完成话题归类时可能不会展示事件脉络入口。
## DB12.5 首页真实数据展示与订阅管理

- 首页左侧新闻分类栏现在通过 `GET /api/news/categories` 从 backend 读取数据库分类。
- 点击分类会跳转 `/home?category_id=xxx`，首页调用 `GET /api/news` 并携带 `category_id` 重新加载真实新闻。
- 首页右侧新闻热榜 Top10 调用 `GET /api/news/hot`，按浏览、点赞、收藏、评论综合热度排序。
- 无封面新闻采用纯文本卡片，不再显示大块“暂无封面”占位。
- 左侧“订阅管理”会调用 `GET /api/profile/subscriptions` 和 `POST /api/profile/subscriptions`，需要登录后使用。
- 当前前端仍只调用 backend，不直接连接数据库，也不直接调用 ai-service。
